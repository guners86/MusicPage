<div class="row">
	
	<% @artists.each do |artist| %>

		<div class="col-lg-3 col-md-4 col-xs-6">
			<div class="thumbnail">
			   	<img class="img-responsive" src="<%= artist.image_url %>" alt="">
			   	<div class="caption">
			       	<h3 class="text-center"><%= artist.name %></h3>
			       	<p>
		        		<a href="<%= artist_path(artist) %>" class="btn btn-primary btn-lg btn-block" role="button">Ver más</a>
		        	</p>
			    </div>
			</div>
		</div>
	
	<% end %>
	
</div>


