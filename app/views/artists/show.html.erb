<div class="row">
    <div class="col-md-8">
        <h1><%= @artist.name %></h1>
        <h3><%= "Albums(" + @artist.albums.count.to_s + ")" %></h3>
    </div>
    <div class="col-md-4">
        <a class="thumbnail" href="#">
	        <img class="img-responsive" src="<%= @artist.image_url %>" alt="">
	    </a>
    </div>
</div>